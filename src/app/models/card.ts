import { ListCard } from './listCard';

export class Card {
    cardId: string;
    cardTitle: string;
    description: string;
    dueDate: string;
    commentText: string;
    cardModel: ListCard[];
    commentId: string;

}
